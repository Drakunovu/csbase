; =========================================================
; METAMOD PLUGINS CONFIGURATION FILE
;
; --- ACTIVATION / DEACTIVATION INSTRUCTIONS ---
; 1. TO DEACTIVATE a plugin: Place a semicolon (;) at the start of the line.
; 2. TO ACTIVATE a plugin: Ensure the line does not start with a semicolon (;)
;    AND the full, correct path to the plugin's .so/.dll file is present.
; =========================================================

; --- CORE FRAMEWORKS ---
; These are essential components that other plugins rely on.

; AMX Mod X (Standard scripting engine)
linux addons/amxmodx/dlls/amxmodx_mm_i386.so

; --- ANTI-CHEAT & SECURITY MODULES ---

; Reunion (Protocol/Engine version compatibility)
linux addons/metamod/dlls/reunion/reunion_mm_i386.so
; ReAuthCheck (Authentication Fixes/Checks for newer clients)
linux addons/metamod/dlls/reauthcheck/reauthcheck_mm_i386.so
; ReChecker (Anti-Cheat/File Integrity checking)
linux addons/metamod/dlls/rechecker/rechecker_mm_i386.so

; --- GAMEPLAY FIXES & ENHANCEMENTS ---

; ReVoice (Voice communication fix for newer clients)
linux addons/metamod/dlls/revoice/revoice_mm_i386.so
; Hitbox Fix (Fixes hitbox alignment issues)
linux addons/metamod/dlls/hitboxfix/hitbox_fix_mm_i386.so
; ReSemiClip (Allows players to walk through teammates in non-competitive modes)
linux addons/metamod/dlls/resemiclip/resemiclip_mm_i386.so

; --- END OF PLUGINS ---